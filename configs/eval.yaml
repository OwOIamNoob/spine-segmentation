# @package _global_

defaults:
  - _self_
  - data: spider_semantic # choose datamodule with `test_dataloader()` for evaluation
  - model: spider
  - logger: wandb
  - trainer: gpu
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path:  /work/hpc/spine-segmentation/logs/cache/2024-10-16_19-48-52/checkpoints/last.ckpt

data:
  batch_size: 1