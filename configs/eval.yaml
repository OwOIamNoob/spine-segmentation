# @package _global_

defaults:
  - _self_
  - data: spider_coccyx # choose datamodule with `test_dataloader()` for evaluation
  - model: spider_coccyx
  - logger: wandb
  - trainer: gpu
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path:  null
